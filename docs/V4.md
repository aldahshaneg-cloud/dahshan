# الجيل الرابع (v4) — واجهات الدهشان على Laravel/Blade

طلب صاحب النظام 2026-09-21: «أريد التطوير الرابع… كل شيء على لارافل… نبدأ بتطبيق الكول سنتر فقط لكي لا
نعطل سير العمل، تطبيق موازي للموجود وتترك الموجود كما هو حتى يكتمل الجديد». المرجع في الشكل والبنية:
«الشمس — Laravel» (`G:\shams-laravel`) اللي عجب صاحب النظام.

## الفكرة في سطرين

السيرفر كان Laravel من زمان؛ اللي مكانش Laravel هو **الواجهات**: ملف HTML واحد لكل لوحة (الكول سنتر 690 كيلو)
بيسحب ألف أوردر ويعدّ ويفلتر في المتصفح. v4 = شاشة Blade لكل صفحة + ملف JS/CSS لكل موديول + **القراءات
التقيلة في السيرفر** (تجميع، فلترة، بحث، ترقيم). الكتابة كلها على نفس مسارات `/api/*` بقواعدها وحرّاسها.

## الرابط

`/v4/login` → بيحوّل كل دور لتطبيقه · الكول سنتر: `/v4/callcenter` (شغال على كل الدومينات).
الدخول **واحد** للقديم والجديد (نفس كوكي الجلسة). القديم `callcenter.html` ماتلمسش.

## البنية

```
config/v4.php                         التطبيقات والأدوار والصفحات (القائمة الجانبية بتتبني منه) + تعريف قوايم الأوردرات
routes/v4.php                         مسارات /v4 — من غير closures (route:cache) وبرّه بوابة route:coverage
bootstrap/app.php                     مجموعة `v4` (جلسة النظام من غير EncryptCookies) + alias `v4.auth`
app/Http/Middleware/V4Auth.php        `v4.auth:<app>`: من غير جلسة ← /v4/login?next= · دور غلط ← 403 بصفحة
app/Http/Controllers/V4/              AuthController (شاشة الدخول والتوجيه) · CallcenterController (شاشات + JSON قراءة)
app/Services/V4/
  CallcenterStats                     أرقام الرئيسية والفروع وشارات القائمة — استعلامات تجميع بيوم العمل
  OrderListQuery                      قوايم الأوردرات: فلترة/بحث/ترقيم في السيرفر (30 صف + إجمالي) بشكل OrderWire
  ContactSearch                       بحث دفتر العملاء (اسم بأي ترتيب / تليفون منسوخ) + آخر منطقة وعنوان
resources/views/v4/layouts/app        القالب: شريط جانبي + شريط علوي + وضع ليلي + نافذة تفاصيل الأوردر
resources/views/v4/callcenter/*       شاشة لكل صفحة
public/v4/css/app.css                 لغة التصميم (متغيّرات للوضع الداكن) · cairo + fontawesome محليين
public/v4/js/core.js                  V4.api · toast · modal · confirm · live (نفس realtime.js) · fmt · cleanPhone · norm
public/v4/js/orders-common.js         صف الأوردر الموحّد + نافذة التفاصيل
public/v4/js/callcenter/embed.js      عرض شاشة القديم بكودها نفسه جوه الغلاف (قاعدة embed تحت)
public/v4/js/callcenter/<page>.js     سكربت كل شاشة
ops/test_v4_callcenter.php            الحارس (61 فحص)
```

## القواعد

1. **الكتابة على `/api/*` بس.** ممنوع منطق شغل جديد في كنترولرات v4 — يا إما مسار `/api` موجود، يا إما يتضاف
   هناك بحرّاسه. (الترقيم، تفريق الطرود، سقف العهدة، منع التكرار `clientRef`… كلها في السيرفر ومتجرّبة.)
2. **القراءات التقيلة خدمة في `app/Services/V4`** بترجّع أشكال السلك الموجودة (`OrderWire`…) — لسان واحد.
3. **اليوم = يوم العمل** (٩ص→٩ص القاهرة، `BizDay`) في كل رقم وكل فلتر تاريخ.
4. **النوافذ:** اللي فيها خانات مابتتقفلش بالضغط برّه؛ الإجبارية `data-locked`؛ العرض بس بتتقفل. التوست فوق في النص.
5. **البث:** `V4.live.onOrders/onRequests` — الحدث جرس، الشاشة بتعيد تحميل اللي قدامها. الطلب الشغّال ← علامة
   «دورة كمان» من غير مؤقتات (درس 2026-09-21). استطلاع أمان كل 60 ث + رجوع التبويب.
6. **مفيش npm/Vite.** JS عادي، ملف لكل شاشة، كسر الكاش بـ`filemtime` في الـBlade.
7. **صفحة لسه ماتنقلتش** = `ready => false` في `config/v4.php` ← «قريبًا» بتفتح القديم. ممنوع شاشة نص شغالة.
8. ⚠️ ملفات PHP فيها `\` تتكتب بأداة Write/Edit مش بـheredoc/`node -e` (الـbackslash بيتاكل).

## 🔴 «أمور لا أريد تغييرها» — قاعدة `embed` (قرار صاحب النظام 2026-09-21 بعد أول عرض)

أول نسخة من v4 أعادت تصميم «طلب جديد» (صفحة مستقلة) و«الطلبات النشطة». صاحب النظام رفض: «في الإصدار السابق
هناك أمور لا أريد تغييرها مثل عمل أوردر جديد وصفحة الأوردرات النشطة». (وفي كود القديم قرار أقدم منه: شاشة
«أوردر جديد» المستقلة اتشالت والفورم مكانه **مودال** جوه صفحة الطلبات.)

**القاعدة:** الشاشة اللي الموظفين متعوّدين عليها **ماتتعادش تصميمها**. في `config/v4.php` الصفحة اللي عليها
`'embed' => '<صفحة القديم>'` بتتعرض **بكود `callcenter.html` نفسه** جوه غلاف v4:
`callcenter.html?embed=<page>[&new=1]&theme=…` — وضع خامل اتضاف للقديم (من غير الباراميتر الصفحة زي ما هي):
بيخفي قايمته هو، يفتح الصفحة المطلوبة، يفتح مودال الأوردر لو `new=1`، ويبلّغ الغلاف بـ`postMessage`
(`ready` / `login`). الغلاف (`embed.js`) بيظبط كاش الجلسة `tiar-session`، يوحّد الوضع الليلي، وزرار «طلب جديد»
فوق بيفتح مودال القديم نفسه. النقل الأصلي لأي شاشة من دول لازم يكون **مطابق للقديم** ويتعرض على صاحب النظام
الأول — وتحويل الصفحة من/إلى «زي القديم» = سطر واحد في الإعدادات.

## حالة الكول سنتر (كله جاهز — مفيش «قريبًا»)

| شاشات Blade أصلية (تحسين في السيرفر) | شاشات القديم زي ما هي جوه الغلاف (`embed`) |
|---|---|
| الرئيسية · بحث سريع · قيد التوصيل · المسلَّمة · لم يتم التوصيل · الملغاة · الطيارين · دليل المناطق · العملاء | طلب جديد (مودال) · الطلبات النشطة · خريطة الطيارين · رسايل العملاء · الشكاوى · أدائي |

## إضافة تطبيق جديد (الفرع/الإدارة…)

`config/v4.php` (تطبيق + أدوار + صفحات) ← `routes/v4.php` (مجموعة `v4.auth:<app>`) ← كنترولر + views + js.
القالب والنواة والحارس بيتشاركوا زي ما هم.
