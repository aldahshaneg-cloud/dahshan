/* ⚠️ متنفّذ 2026-08-31 — سكربت لمرة واحدة، **اتنفّذ خلاص**.
   النصوص اللي جواه بتوصف الكود *قبل وبعد* التعديل ساعتها، وبعدها اتعدّل
   تاني. أي مراجعة تقرا الكود الحيّ في PilotAppController.php مش من هنا. */
/* الطيار مايقدرش ينهي ورديته — الإنهاء بقى للفرع والإدارة بس.
 *
 * ═══ القرار (صاحب النظام 2026-08-30) ═══
 * «امنع الطيار إنه يخرج من الوردية — اللي يخرجه هو الفرع أو الإدارة».
 *
 * ═══ ليه ده أصح ماليًا كمان ═══
 * `PilotAppController::shiftEnd` كانت بتقفل الوردية **من غير أي تسوية
 * فلوس** — لا عهدة ولا خزنة ولا `money_settled` (مكتوب في تعليقها).
 * أما `BoardController::shiftEnd` (الفرع/الإدارة) فبتنده `settlePilotMoney`
 * وبتفرض تسوية الأوردرات المسلّمة الأول.
 * يعني الطيار كان يقدر يقفل ورديته وفلوسه معلّقة. دلوقتي كل قفل بيعدّي
 * على التسوية.
 *
 * ═══ ليه المسار بيفضل موجود ═══
 * ① `pilot/shift/end` جزء من **العقد الأصلي** — لو اتشال، `route:coverage`
 *    بيقول «ناقص ١» وبوابة الترحيل بتقع.
 * ② مافيش فرض تحديث على الإنتاج (`site_settings.pilotApp` مش موجود أصلًا)،
 *    فالتطبيقات المثبّتة على تليفونات الطيارين لسه هتنده عليه. الرد الواضح
 *    أحسن من 404 غامض.
 *
 * فالمسار بيفضل ويرفض برسالة مفهومة.
 */
const fs = require('fs');
const f = 'app/Http/Controllers/Api/PilotAppController.php';
let s = fs.readFileSync(f, 'utf8');
const eol = s.includes('\r\n') ? '\r\n' : '\n';
if (eol === '\r\n') s = s.replace(/\r\n/g, '\n');

const OLD_DOC = `    /**
     * POST /api/pilot/shift/end — إنهاء الوردية من التطبيق نفسه.
     *
     * نفس سلوك التطبيق القديم حرفيًا (closeShift + releasePilotFromBranch):
     * قفل سجل الوردية → تعليمها \`ended\` → قفل صف الطيار → التحرير الكامل
     * من الفرع وإزاحة الدور.
     *
     * 🔴 **من غير أي تسوية فلوس** — لا عهدة ولا خزنة ولا \`money_settled\`.
     * التسوية بتفضل شغلة الفرع من اللوحة (\`POST /api/shifts/{id}/end\`
     * اللي بينده \`settlePilotMoney\`). ده مش سهو: الطيار بيقفل ورديته من
     * التطبيق وهو في الشارع، والفلوس بتتسلّم في الفرع بعدين.
     *
     * ⚠️ بيشتغل **حتى لو مفيش وردية مفتوحة** — بيحرّر الطيار بس. زي القديم.
     * ⚠️ \`ended_by = 'pilot'\` نص حرفي مش اسم المستخدم — الواجهة بتفرّق.
     * ⚠️ ترتيب القفلين (الوردية ثم الطيار) جزء من الصح، متغيّرش.
     */`;

const NEW_DOC = `    /**
     * POST /api/pilot/shift/end — **مقفول**.
     *
     * ═══ القرار (صاحب النظام 2026-08-30) ═══
     * «امنع الطيار إنه يخرج من الوردية — اللي يخرجه هو الفرع أو الإدارة».
     *
     * ═══ ليه ده أصح ماليًا ═══
     * الدالة دي كانت بتقفل الوردية **من غير أي تسوية فلوس** — لا عهدة ولا
     * خزنة ولا \`money_settled\`. يعني الطيار يقفل ورديته وفلوسه معلّقة،
     * والفرع يكتشفها في التقفيلة. البديل (\`POST /api/shifts/{id}/end\` →
     * \`BoardController::shiftEnd\`) بينده \`settlePilotMoney\` وبيفرض تسوية
     * الأوردرات المسلّمة الأول. فكل قفل دلوقتي بيعدّي على التسوية.
     *
     * ═══ ليه المسار فاضل موجود بدل ما يتشال ═══
     * ① جزء من **العقد الأصلي** — شيله بيخلّي \`route:coverage\` يقول «ناقص ١».
     * ② مافيش فرض تحديث على الإنتاج (\`site_settings.pilotApp\` مش موجود)،
     *    فالتطبيقات القديمة على تليفونات الطيارين لسه بتنده عليه. رسالة
     *    واضحة أحسن من 404 غامض.
     *
     * التطبيق الجديد شال زر «إنهاء الوردية» وشال النداء من تسجيل الخروج،
     * فالمسار ده مابيتندهش منه أصلًا — هو بس شبكة أمان للنسخ القديمة.
     *
     * 🔒 الحارس: ops/test_shift_end_lock.php
     */`;

const OLD_BODY = `    public function shiftEnd(Request $request): JsonResponse
    {
        $pilot   = $this->pilotCtx($request);
        $pilotId = (int) $pilot['id'];

        try {`;

const NEW_BODY = `    public function shiftEnd(Request $request): JsonResponse
    {
        /* 🔒 مقفول: الطيار مايقدرش ينهي ورديته. بننده \`pilotCtx\` الأول
           عشان الحساب اللي مش مربوط بطيار ياخد 403 زي أي مسار طيار تاني
           — الرد يفضل متسق مهما كان سبب الرفض. */
        $this->pilotCtx($request);

        throw new ApiException(
            'إنهاء الوردية بقى من الفرع أو الإدارة — كلّم المشرف بتاعك',
            403
        );
    }

    /**
     * الجسم القديم — متساب للتوثيق ومش بيتنده.
     *
     * لو اتقرر يومًا إن الطيار يرجع ينهي ورديته، القرار محتاج يتعمل مع
     * تسوية الفلوس مش من غيرها (شوف \`settlePilotMoney\` في BoardController).
     */
    private function shiftEndLegacyUnused(int $pilotId): void
    {
        try {`;

let bad = 0;
const one = (old, neu, label) => {
  const n = s.split(old).length - 1;
  if (n !== 1) { console.log(`  🔴 ${label}: اتلقت ${n} مرة`); bad++; return; }
  s = s.replace(old, neu);
  console.log('  ✓ ' + label);
};

one(OLD_DOC, NEW_DOC, 'التوثيق');
one(OLD_BODY, NEW_BODY, 'الرفض + عزل الجسم القديم');

/* الجسم القديم كان بيرجّع JsonResponse — بقى void، فالـreturn لازم يتشال */
one(`            report($e);
            throw new ApiException('تعذّر إنهاء الوردية — جرّب تاني', 500);
        }

        return ApiResponse::ok();
    }`,
`            report($e);
            throw new ApiException('تعذّر إنهاء الوردية — جرّب تاني', 500);
        }
    }`,
'إرجاع الجسم القديم');

fs.writeFileSync(f, eol === '\r\n' ? s.replace(/\n/g, '\r\n') : s);
console.log(bad ? `\n🔴 ${bad} مشكلة` : '\n✅ السيرفر اتقفل');
process.exit(bad ? 1 : 0);
