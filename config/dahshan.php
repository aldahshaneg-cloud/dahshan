<?php

/*
 * إعدادات خاصة بنظام الدهشان — القيم دي جزء من العقد مع الواجهات
 * وتطبيق الطيار، مش تفضيلات. أي تغيير فيها لازم يعدّي على STATE.md.
 */
return [
    // APP_VERSION() في api/config.php — بترجع في رد /api/login و /api/me
    'version' => env('DAHSHAN_VERSION', '1.0.0'),

    // خانق الدخول — جدول login_attempts (api/routes/auth.php)
    'login' => [
        'max_fails'  => 8,    // LOGIN_MAX_FAILS  — محاولات فاشلة قبل القفل
        'window_min' => 15,   // LOGIN_WINDOW_MIN — نافذة تجميع الفشل بالدقايق
        'lock_min'   => 15,   // LOGIN_LOCK_MIN   — مدة القفل بالدقايق
    ],

    /* ═══════════════════════════════════════════════════════════════
       رسالة الواتساب للمستلم — جدول order_notifications
       ═══════════════════════════════════════════════════════════════

       المفتاح الوحيد اللي بيقلب النظام من «يدوي منظّم» لـ«تلقائي» هو
       `provider`. الباقي بيانات اعتماد Meta ومابيتقريش أصلًا في الوضع
       اليدوي.

       manual    — الافتراضي دلوقتي. المهمة بتسجّل الصف بحالة pending وبس،
                   والموظف بيبعت بإيده من شاشة أكواد الشحنة (store.html).
       cloud_api — بيبعت فعلًا لـ Meta Graph API. **متكتوب كامل ومش مفعّل**:
                   من غير token و phone_id المزوّد بيرفض يشتغل ويرجّع سبب
                   مكتوب في عمود error بدل ما يرمي استثناء.

       🔴 تشغيل التلقائي = تغيير WHATSAPP_PROVIDER=cloud_api في .env + ملّي
       WHATSAPP_TOKEN و WHATSAPP_PHONE_ID، وبعدها `php artisan config:cache`.
       مافيش سطر كود بيتغيّر.
    */
    'whatsapp' => [
        // manual | cloud_api
        'provider' => env('WHATSAPP_PROVIDER', 'manual'),

        // بيانات Meta Cloud API — فاضية عن قصد لحد ما الحساب يجهز
        'token'    => env('WHATSAPP_TOKEN', ''),
        'phone_id' => env('WHATSAPP_PHONE_ID', ''),

        /* اسم قالب Meta المعتمد. فاضي = نبعت رسالة نصية حرة، وهي **مابتوصلش
           لعميل جديد** لأن Meta بتسمح بالنص الحر جوه نافذة الـ24 ساعة بس.
           يعني الإطلاق الحقيقي محتاج قالب معتمد هنا. */
        'template' => env('WHATSAPP_TEMPLATE', ''),

        // لغة القالب عند Meta — العربي عندهم 'ar'
        'template_lang' => env('WHATSAPP_TEMPLATE_LANG', 'ar'),

        // نسخة Graph API — بتتثبّت عشان ترقية Meta ماتغيّرش السلوك فجأة
        'graph_version' => 'v21.0',

        // ثواني انتظار نداء Meta — أقصر من مهلة العامل عشان الفشل يتسجّل مش يتعلّق
        'timeout' => 15,

        /* 🔴 **مطلق ومثبّت — ممنوع يتبني من APP_URL.**
           اللينك ده بيتطبع في QR على الطرد نفسه، فلو اتبنى من نطاق الطلب كان
           هيتكسر أول ما الصفحة تتفتح من نطاق تاني. نفس القيمة بالحرف اللي في
           `trackUrlOf()` في public/store.html — أي تغيير هنا لازم يتغيّر هناك. */
        'track_base' => 'https://aldahshan.cloud/',
    ],

    /* ═══════════════════════════════════════════════════════════════
       إشعارات ستارة الهاتف لتطبيق العملاء — Web Push (VAPID)
       ═══════════════════════════════════════════════════════════════

       الجداول: customer_push_subscriptions (أجهزة العميل) و
       customer_push_events (منع التكرار). الإرسال في App\Jobs\SendCustomerPush
       وبيتنده من BroadcastsOrders::broadcastOrder() مع أي تغيير على أوردر.

       🔴 المفتاحين فاضيين = الميزة **متعطّلة بالكامل**: مسار /push/key بيرد
       503، والمهمة بتخرج قبل ما تلمس القاعدة. تشغيلها = توليد زوج VAPID
       على السيرفر نفسه (الأمر مكتوب في .env.example) + config:cache.

       ⚠️ الزوج بيتولّد **لكل بيئة لوحده** وممنوع ينتقل من التطوير للإنتاج.
       وتغييره بعد التشغيل بيبطل كل الاشتراكات القديمة (المتصفح بيربط
       الاشتراك بالمفتاح العام) — العملاء لازم يفعّلوا الإشعارات تاني.
    */
    'push' => [
        // زوج VAPID — base64url زي ما VAPID::createVapidKeys() بترجّعه
        'public_key'  => env('VAPID_PUBLIC_KEY', ''),
        'private_key' => env('VAPID_PRIVATE_KEY', ''),

        // جهة الاتصال اللي خدمة الدفع بتشوفها في توقيع VAPID (mailto: أو https://)
        'subject' => env('VAPID_SUBJECT', 'mailto:info@aldahshan.cloud'),

        /* كام ثانية خدمة الدفع تحتفظ بالإشعار لو الجهاز مقفول/أوفلاين.
           6 ساعات: «تم التسليم» لسه مفيد بعد ساعات، و«الطيار في الطريق»
           بعد يوم كامل مضلّل أكتر منه مفيد. */
        'ttl' => 21600,

        // high = الجهاز يصحى ويعرض فورًا (الحالة مرتبطة بوقت حقيقي — الطيار على الباب)
        'urgency' => 'high',

        // ثواني انتظار نداء خدمة الدفع — أقصر من مهلة العامل عشان الفشل يتسجّل مش يتعلّق
        'timeout' => 15,
    ],

    /*
    |---------------------------------------------------------------------
    | 🚨 تنبيهات الأعطال
    |---------------------------------------------------------------------
    | العطل بيتسجّل في `error_alerts` مجمّعًا بالبصمة، وبيتبعت واتساب لأرقام
    | الإدارة. الجدول هو مصدر الحقيقة — الواتساب طبقة تنبيه فوقه.
    |
    | 🔴 الإرسال الفعلي محتاج `WHATSAPP_PROVIDER=cloud_api` مع token و
    | phone_id. من غيرهم المزوّد بيفضل `manual` والتنبيه بيتسجّل ويبان في
    | لوحة الإدارة **من غير** ما رسالة توصل — وده متعلّم في `notify_status`.
    */
    'alerts' => [
        'enabled' => (bool) env('ALERTS_ENABLED', true),

        // أرقام مفصولة بفواصل — 01xxxxxxxxx
        'whatsapp_to' => env('ALERTS_WHATSAPP_TO', ''),

        /* نفس العطل مابينبّهش تاني قبل المدة دي. 60 دقيقة: مسار مكسور
           بيفضّل يرن كل ساعة (تذكير حقيقي) مش كل ثانية (ضجيج بيتقفل). */
        'cooldown_minutes' => (int) env('ALERTS_COOLDOWN_MINUTES', 60),

        /* سقف التنبيهات في الساعة عبر **كل** الأعطال. الغرض إن انهيار عام
           (قاعدة واقعة مثلًا) مايبعتش عشرات الرسايل — أول 6 كفاية عشان
           تعرف إن في حاجة كبيرة، والباقي متسجّل في الجدول. */
        'max_per_hour' => (int) env('ALERTS_MAX_PER_HOUR', 6),
    ],
];
