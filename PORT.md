# PORT.md — حالة ترحيل «الدهشان» إلى Laravel

> **قاعدة صارمة (نفس قاعدة `aldahshan/STATE.md`):** ممنوع اعتبار أي بند
> «خلصان» من غير دليل تحقق قابل للتشغيل مكتوب جنبه.
>
> بوابات القبول — كل واحدة بتقارن بالأصل مش بالتوقّع:
> ```
> powershell -File tests\parity.ps1     # القراءة: النظامين جنب بعض، رد بـرد
> php artisan route:coverage            # التغطية مقابل جدول المسارات الأصلي
> php artisan schema:verify             # السكيمة جدول بجدول
> php tests\diff_vocab.php              # القاموس دالة بدالة
> php artisan wire:verify               # طبقة السلك على بيانات حقيقية
> php artisan wire:edge                 # طبقة السلك على حالات حدّية
> ```
> والكتابة بتتحقق بتوجيه اختبارات النظام القديم نفسها على لارافل:
> ```
> cd ..\aldahshan
> $env:ALDAHSHAN_TEST_TARGET = 'laravel'
> powershell -File tests\e2e_phase1.ps1        # 27 خطوة دورة حياة الشحنة
> powershell -File tests\e2e_phase2_pilot.ps1  # 31 خطوة تطبيق الطيار
> powershell -File tests\e2e_damascus.ps1      # 39 فحص حسابات روح دمشق
> ```
> ## 🎯 آخر تشغيلة كاملة — 2026-08-19
>
> **التغطية: 219 / 219 مسار (100%) · ناقص 0 · زيادة 0**
>
> | البوابة | النتيجة |
> |---|---|
> | `route:coverage` | **219/219 (100%)** |
> | `parity.ps1` (القراءة، رد بـرد) | **241 / 241** |
> | `schema:verify` | **82 جدول، صفر اختلاف** |
> | `diff_vocab` | **474 مقارنة، صفر اختلاف** |
> | `wire:verify` · `wire:edge` | **22/22** · **13/13** |
>
> **والكتابة — اختبارات النظام القديم نفسها بتتنفّذ على لارافل:**
>
> | الطقم | على لارافل |
> |---|---|
> | `e2e_phase1` (دورة حياة الشحنة، 27 خطوة) | **27 / 27** |
> | `e2e_phase2_pilot` (تطبيق الطيار) | **31 / 31** |
> | `e2e_damascus` (حسابات روح دمشق) | **39 / 39** |
> | `e2e_trust` (الخصوصية العدائي) | **117 / 117** |
> | `e2e_incoming_wallet` (قفل الوارد + رد المحفظة) | **28 / 28** |
>
> **والنظام القديم لسه كله أخضر (صفر انحدار):**
> `smoke_all` 55/55 · `e2e_phase1` 27/27 · `e2e_phase2_pilot` 31/31 ·
> `e2e_trust` 117/117 · `e2e_damascus` 39/39 · `incoming_wallet` 28/28 ·
> `diff_damascus` صفر فروق

### 🔴 فخ نشر خطير في Laravel Broadcasting — اتحل (2026-08-19)

`php artisan install:broadcasting` بيحط `channels: routes/channels.php` جوه
`withRouting`، واللي بتعمل `require` لملف القنوات على **كل طلب**.
و`Broadcast::channel()` بتعدّي على `BroadcastManager::__call` اللي **بيبني
السائق** (Pusher + Guzzle) عشان يسجّل القناة.

النتيجة: **الـ219 مسار كلهم بيبنوا عميل Pusher على كل طلب، وأي مفتاح Reverb
ناقص بيرمي 500 على كل مسار في النظام — حتى `/api/health`.** ده مش افتراض:
اتشاف فعليًا مرتين ساعة التثبيت، والمرة التانية أوقفت كل أوامر artisan
(`--version` و`inspire` كمان).

الحل: تحميل القنوات **اتأجّل** لـmiddleware `BroadcastActor` اللي بيشتغل على
مسار `/broadcasting/auth` وحده. `Broadcast::routes()` نفسها رخيصة —
بتسجّل مسار ومابتلمسش السائق.

**التحقق**: اتشال `REVERB_APP_KEY` من `.env` عمدًا — `artisan attendance:autoclose`
و`GET /api/health` **الاتنين فضلوا شغالين**. من غير الإصلاح ده كان النظام كله
بيقع.

ونقطتين تانيتين اتحلّوا في نفس الشغل:
- مسار `/broadcasting/auth` الافتراضي بيتحط على مجموعة **web** وفيها
  `EncryptCookies` — وكوكي الجلسة عندنا **مش مشفّر**. كان هيفشل يقرا الجلسة
  وكل اشتراك في قناة خاصة يترفض 403. اتحط على مجموعة `api`.
- المسار متساب **برّه بادئة `/api`** عن قصد — `route:coverage` بيقارن كل
  مسار `api/*` بالجدول الأصلي، فأي مسار جديد تحتها كان هيتحسب «زيادة»
  ويكسّر بوابة الـ219/219.

## القرار والسبب

`aldahshan/REBUILD.md:13` فيه قرار متسجّل: «API: PHP نقي بدون فريمورك
(يشتغل على أي استضافة)». **القرار ده اتعكس يوم 2026-08-19 بطلب صاحب
المشروع** بعد ما اتحدد إن السيرفر هيبقى VPS (مش استضافة مشتركة)، وإن
الباقي من المنظومة (HR والمحاسبة والتحديث الفوري) هو الجزء الأكبر ولسه
ما اتبناش — وده اللي الفريمورك بيوفّر فيه فعليًا.

## المبادئ الحاكمة

1. **عقد الـAPI مجمّد.** نفس المسارات، نفس مفاتيح الـJSON، نفس ترتيبها،
   نفس الترميز (`JSON_UNESCAPED_UNICODE | JSON_UNESCAPED_SLASHES`). أي
   اختلاف = انحدار حتى لو «أنضف».
2. **السكيمة بتتحافظ عليها** — نفس الـ81 جدول، مش إعادة تصميم.
3. **اختبارات النظام الحالي هي بوابة القبول.** كلها HTTP خالص
   (curl على سيرفر مشغّل)، فبتشتغل على لارافل زي ما هي طول ما العقد ثابت.
4. **النظام الحالي مايتلمسش.** بيفضل شغّال، ولارافل بيتبني جنبه.

## المسارات المكانها

| | |
|---|---|
| النظام الحالي (نسخة العمل) | `D:\dahshaneg\aldahshan` |
| النظام الحالي (نسخة الرفع) | `C:\Users\hp\aldahshan` |
| لارافل | `D:\dahshaneg\dahshan` |
| قاعدة البيانات | `aldahshan` — **مشتركة بين الاتنين** |

## ✅ خلصان — بدليل

| البند | الدليل |
|---|---|
| مشروع Laravel 12.67 على PHP 8.2.12 | `php artisan --version` · متصل بـ`aldahshan` (81 جدول) · `EMULATE_PREPARES=0` · توقيت الاتصال `SYSTEM` (مش متحدد) زي الأصل |
| غلاف الرد `ApiResponse` | العلمان `JSON_UNESCAPED_UNICODE\|JSON_UNESCAPED_SLASHES` — العربي بيرجع خام. **ده مش شكلي**: فحص الخصوصية العدائي بيمشّط نص الرد الخام على أسماء عربية |
| `ApiException` بدل `fail()` | استثناء مش `exit` — فـ`DB::transaction()` بتعمل rollback لوحدها بدل الاعتماد على `rollBack()` يدوي قبل كل `fail()` |
| معالج الأخطاء | الرسايل التلاتة من `public/index.php` حرفيًا: «خطأ في قاعدة البيانات» 500 · «حدث خطأ غير متوقع» 500 · «المسار غير موجود» 404 |
| مجموعة `api` من غير CSRF ومع جلسة | النظام مافيهوش CSRF أصلًا والمصادقة كوكي جلسة — `StartSession` متضاف يدويًا و`web` group مستبعدة |
| `TolerantJsonBody` | جسم فاضي أو JSON مكسور = `[]` مش 400 — زي `body_json()`. مسارات كتير بتتنادى بلا جسم |
| `ResolveApiActor` | ترتيب المصادقة التلاتي بالحرف: جلسة عميل ← جلسة موظف ← `X-Auth-Token`. الحظر بيتقرا من القاعدة كل طلب، **والصف المحذوف = محظور** |
| `EnsureRole` | مقارنة صارمة على الكود الإنجليزي · بلا دخول = **401** مش 403 |
| `LoginThrottle` | جدول `login_attempts` زي ما هو (**مش** `RateLimiter`) — 8 محاولات / نافذة 15 د / قفل 15 د |
| `/api/health` `/api/login` `/api/logout` `/api/me` | **مطابقة حرفيًا للنظام القديم** — `tests\parity.ps1` = 6/6 |
| كوكي الجلسة | `ALDAHSHAN_SESS` بـ HttpOnly — نفس اسم الأصل (تطبيق الطيار والواجهات بيعتمدوا عليه) |
| **السكيمة — 82 جدول** | `database/schema/mysql-schema.sql` مأخوذ من القاعدة الحية بـ`mysqldump --no-data`. `php artisan schema:verify` بيحمّله في قاعدة مؤقتة ويقارن `SHOW CREATE TABLE` لكل جدول = **صفر اختلاف**. و`php artisan migrate` على قاعدة **فاضية تمامًا** طلّع 82 جدول مطابقين للأصل حرفيًا — يعني مسار النشر على الـVPS مثبت |
| **جلسات العميل في الاختبارات** | `php artisan test:seed-customer-sessions` بيزرع نفس فيكستشرز `seed_incoming_fixtures.php` وبيفتح 3 جلسات لارافل حقيقية بـ`SessionManager` (مش كتابة ملفات باليد — عشان يفضل شغّال لو السواقة اتحوّلت لـ`database`)، وبيطبع **نفس مفاتيح الـJSON**. `e2e_incoming_wallet` بقى **28/28 على الهدفين**. أداة محلية: بترفض الاشتغال على `APP_ENV=production` |
| **القاموس** (`Vocab`/`WireTime`/`OrderNumber`/`Commission`) | `tests/diff_vocab.php` بيحمّل **الكود القديم نفسه** جنب الكلاسات الجديدة ويقارن ناتج كل دالة على مدخلات صالحة وغريبة ومجهولة = **474 مقارنة، صفر اختلاف**. شامل: 144 تركيبة لمعادلة عمولة الطيار، و6 طوابع وقت حوالين منتصف الليل بتوقيت القاهرة، و16 خريطة نصية |

### ملاحظات على قرارات صغيرة اتاخدت

- **الهاش الوهمي لحماية التوقيت** بقى 60 حرف (صالح) بدل 61. اتقاس في
  النظام القديم يوم 2026-08-19: الزمنين 69.3 و71.6 مللي — يعني الحماية
  كانت شغالة رغم الطول الغلط، فده تنضيف مش إصلاح ثغرة.
- **`SESSION_DRIVER=file`** مؤقتًا (أقرب لسلوك جلسات PHP الأصلية). يتحوّل
  لـ`database` قبل النشر لو هيبقى فيه أكتر من عامل.

### قرارات صغيرة اتاخدت في السكيمة

- **الميجريشنز الافتراضية بتاعة لارافل اتشالت** (`create_users_table`
  وإخواتها). جدول `users` موجود بالفعل بأعمدة مختلفة تمامًا — تشغيلها كان
  هيصطدم بيه. السكيمة الموجودة هي المرجع.
- **جدول `migrations` اتضاف للقاعدة** (إضافة بحتة، القاعدة بقت 82 جدول).
  الاختبار `smoke_all` بيفحص «77+ جدول» فمفيش تأثير.
- الطريقة المتبعة هي **schema dump** بتاع لارافل مش 81 ميجريشن مكتوبة
  بالإيد. السبب: التحويل اليدوي بيسرّب فروق صامتة (collation عمود، طول
  decimal، ترتيب فهرس، `ON UPDATE CURRENT_TIMESTAMP(3)`)، والـdump بيدّي
  تطابق مثبت. التعديلات الجاية بتبقى ميجريشنز عادية فوقه.

### 🔴 فخ عام في لارافل اتشال — `ConvertEmptyStringsToNull` و `TrimStrings`

لارافل 11+ بيشغّل الاتنين دول **عامًّا على كل طلب** قبل ما الكنترولر يشوف
المدخلات. `ConvertEmptyStringsToNull` بيحوّل `""` إلى `null`، والنظام القديم
**بيفرّق بين الاتنين**: النمط `$_GET['x'] ?? 'افتراضي'` بيرجّع `""` لو
الباراميتر موجود وفاضي (ويترفض بعدين في التحقق)، ويرجّع الافتراضي لو مش
موجود خالص.

اتكشف ببوابة التطابق على `GET /api/wallets?ownerType=` — الأصل بيرد «نوع
المالك غير صالح» ولارافل كان بيرجّع القايمة كاملة. **ده مكانش باج في مسار
واحد — كان فرق سلوك عام على كل مسار بيفرّق بين «فاضي» و«مش موجود».**
الاتنين اتشالوا في `bootstrap/app.php`.

### 37 مسار قراءة اتنقلوا بالوكلاء (2026-08-19)

7 وكلاء اشتغلوا بالتوازي على: الدعم والإعدادات · المالية · تطبيق الطيار ·
لوحة العمليات · العملاء · الموقع العام · منظومة الثقة. كل وكيل قرا الملف
المصدر بالكامل + الملفات النموذج، وكتب الكنترولر + طبقة سلك خاصة بمجاله،
ورجّع سطور فحص. **الدمج والتسجيل والمحاسبة اتعملوا مركزيًا** — الوكلاء
مالمسوش `routes/api.php` عشان مايتصادموش.

الناتج: `SupportController` · `FinanceController` · `PilotAppController` ·
`BoardController` · `CustomersController` · `PublicSiteController` ·
`TrustController` + `SupportWire` · `FinanceWire` · `BoardWire` ·
`CustomerWire` · `PublicWire` · `TrustWire`.

**إجمالي المسارات المسجّلة: 54** (`php artisan route:list --path=api`).

### شذوذات في العقد اتنقلت زي ما هي (مش اتصلحت)

الترحيل صفر تغيير سلوك، فالحاجات دي اتنقلت بعيوبها والإصلاح قرار منفصل:

- **قوايم `support.php` التلاتة** (complaints/zone-requests/partners)
  بترجع `{ok, changed, items}` **من غير `serverNow`** — مخالف للغلاف
  الموحد. فمقدرناش نستخدم `PollableList` معاها.
- **`GET /api/wallets`** كمان من غير `serverNow`.
- **`GET /api/pilot/closeouts`** بيرجّع `{ok, items}` من غير `changed`
  ولا `serverNow`.
- **`GET /api/partners`** مسار عام والاستعلام مافيهوش `WHERE is_active=1`
  — الشريك المخفي بيطلع في الرد العام.
- **`GET /api/settings`** التعليق فوقه بيقول «للموظفين» لكن الكود
  `require_auth()` بس — أي حساب مسجّل (بما فيهم العميل والطيار والمحل)
  بيقرا كل مفاتيح إعدادات النظام.
- **`?status` في قوايم الطلبات** بيتحط في `WHERE` كما هو بلا تحقق،
  والفحص `empty()` مش `!== ''` — يعني `?status=0` بيتجاهل بالكامل.

كلها متسجّلة بتفصيلها في `tests/_agent_notes.md`.

### فخ اختبار اتصلح — تلوث `lookup_log`

بوابة التطابق بتنده `/api/lookup` على النظامين، وكل نداء **بيكتب صف في
`lookup_log`** (الكتابة دي مقصودة — السجل نفسه هو أساس حد الـ60/ساعة).
فتشغيل البوابة قبل `aldahshan/tests/e2e_trust.ps1` كان بيخلي فحص الـ
rate-limit بتاعه يفشل. اتلاحظ فعليًا: **116/117 بدل 117/117**.
البوابة بقت بتسجّل آخر `id` قبل الشغل وتمسح اللي زاد بعده.

### الـ219 مسار كلهم اتنقلوا (2026-08-19)

جولتين وكلاء بالتوازي — 7 وكلاء للقراءة (37 مسار) و10 للكتابة (166 مسار)،
والدمج والتسجيل والمحاسبة اتعملوا مركزيًا. الوكلاء **مالمسوش**
`routes/api.php` عشان مايتصادموش، والتسجيل اتولّد آليًا من مخرجهم المهيكل
بترتيب صحيح (الحرفي قبل اللي فيه باراميتر).

| النوع | عدد |
|---|---|
| GET | 73 |
| POST | 106 |
| PUT | 26 |
| DELETE | 15 |
| **الإجمالي** | **219** |

**52 مسار بيلمس فلوس · 85 جوه معاملات** — والتراجع تلقائي لأن `ApiException`
بترمي بدل ما تعمل `exit`، فـ`DB::transaction` بتلغي لوحدها.

الحجم: كنترولرات ~14 ألف سطر · طبقة سلك ~2.7 ألف · دعم ووسطاء ~1 ألف.

## ❌ لسه ما اتعملش

- **Reverb (التحديث الفوري)** — بعد ما العقد يتعادل، مش قبله
- **الواجهات** — لسه بتتقدّم من النظام القديم؛ نقلها لـBlade مرحلة تانية
- **HR والمحاسبة** — مؤجلين بقرار صاحب المشروع (لسه ساري)
- **مراجعة يدوية للمسارات المالية الـ52** — عدّت الاختبارات، بس المراجعة
  البشرية لكل معادلة لسه مطلوبة قبل التشغيل الحقيقي
- طبقة السلك (Serializers) — أخطر جزء: 58 مفتاح في `OrderResource` لازم
  يطلعوا صريحين، ممنوع `when()`/`whenLoaded()`
- Reverb (التحديث الفوري) — بعد ما العقد يتعادل مش قبله
- HR والمحاسبة — **مؤجلين بقرار صاحب المشروع** (لسه ساري)

## ⚠️ معروف ومفتوح

- **الـ42 بند من خريطة التحليل لسه ما اتفحصوش** (`laravel-port-plan.md`).
  نسبة الخطأ على عيّنة 3 بنود كانت 33% — بند من التلاتة طلع إنذار كاذب.
  ممنوع تنفيذ أي بند قبل ما يتأكد بالكود وبالتشغيل.
- `tests/seed_customer_session.php` في النظام القديم لسه بيفتح جلسة PHP
  أصلية ويسلّم الـsession id لـcurl — **مش هيشتغل على لارافل**. المكافئ
  موجود بقى (`php artisan test:seed-customer-sessions`) وبيفتح جلسات
  لارافل حقيقية، بس السكربت ده لسه مش متوجّه عليه لأن مفيش اختبار حالي
  بينده عليه.
