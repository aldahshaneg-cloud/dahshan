<?php

declare(strict_types=1);

namespace App\Console\Commands;

use Illuminate\Console\Command;
use Illuminate\Support\Facades\Route;

/**
 * تغطية الترحيل — بيقارن مسارات لارافل بجدول `$routes` في النظام القديم.
 *
 * المصدر الوحيد للحقيقة هو الجدول الأصلي نفسه (`aldahshan/public/index.php`)
 * مش أي قايمة مكتوبة بالإيد — القايمة اليدوية بتفترق عن الواقع بصمت.
 *
 * فيه نوعين مسار في لارافل مالهمش مقابل في الأصل، ولازم نفرّق بينهم:
 *   • غلطة ترحيل   — اسم أو شكل مسار اتكتب غلط، ده اللي محتاج يتصلّح
 *   • إضافة مقصودة — ميزة جديدة اتعملت بعد الترحيل، مافيش أصل تتقارن بيه أساسًا
 *
 * القايمة البيضا `INTENTIONAL_ADDITIONS` هي اللي بتفصل بين الاتنين: اللي فيها
 * مايتحسبش «زيادة»، بس بيفضل يتعرض في سطر لوحده — مقصود يفضل مراجَع، مش مخفي.
 * لولا ده كانت البوابة هتقول «زيادة N» للأبد وتفقد قيمتها كإشارة.
 *
 * وعشان القايمة ماتتحولش لمقبرة لمسارات اتشالت: أي مسار مكتوب فيها ومش متسجّل
 * فعلًا في لارافل بيطلع تحذير — يا يترجّع يا يتشال من القايمة.
 *
 * بيطلّع:
 *   • الناقص  — مسار في الأصل ومش متسجّل في لارافل
 *   • الزيادة — مسار في لارافل مش في الأصل ولا في القايمة البيضا (غالبًا غلطة اسم)
 *   • إضافات مقصودة — مسار جديد معروف ومكتوب سببه
 *
 * ملحوظة: نسبة التغطية و«الناقص» بيقيسوا الترحيل هو هو — الإضافات المقصودة
 * مابتأثرش عليهم لا بالسالب ولا بالموجب.
 *
 * التشغيل: php artisan route:coverage [--missing] [--extra]
 */
class RouteCoverage extends Command
{
    /**
     * مسارات اتضافت في لارافل بعد الترحيل عن قصد — مالهاش مقابل في النظام القديم.
     *
     * المفتاح: 'METHOD /path' زي ما هو متسجّل (الـ {id} بيتوحّد وقت المقارنة زي أي مسار تاني).
     * القيمة: ده إيه وليه مضاف — أي سطر جديد هنا لازم يجي معاه سبب مكتوب،
     *         القايمة من غير أسباب بتبقى قايمة تجاهل مش قايمة بيضا.
     */
    private const INTENTIONAL_ADDITIONS = [
        /* 📍 دفتر عناوين بوابة المحلات (طلب صاحب النظام 2026-09-02: «في
           تطبيق المحلات اعمل عناويني زي تطبيق العملاء») — مرآة
           customer/addresses بالحرف، والمفتاح صف المحل في users. */
        /* 🗄️ أرشيف طيارين روح دمشق (الجهاز التاني 2026-09-12، اتسجّل هنا 2026-09-13 بعد الاسترجاع):
           الترحيل بيكتب rd_pilots.archived_at بس — التقفيلات القديمة ماتتغيّرش. */
        'GET /api/rd/archive' =>
            'قايمة طيارين روح دمشق المرحّلين للأرشيف مع آخر شهر اشتغلوه',
        'POST /api/rd/pilots/{}/archive' =>
            'ترحيل طيار روح دمشق للأرشيف (archived_at/archived_by/archive_note) — بيختفي من الكشوف الجديدة',
        'POST /api/rd/pilots/{}/unarchive' =>
            'إرجاع طيار روح دمشق من الأرشيف',
        'POST /api/customers/{}/price-edit' =>
            'فتح/قفل تعديل سعر التوصيل للعميل زي المحلات (طلب 2026-09-02) — المنح من إدارة العملاء والبوابة الفعلية في customerDeliveryPrice',
        /* المسار الفعلي للمحلات (users.can_edit_price) — كان موجود من 2026-09-03
           ومش مسجّل هنا فالبوابة كانت بتعدّه «زيادة». اتسجّل 2026-09-07. */
        'POST /api/users/{}/price-edit' =>
            'فتح/قفل تعديل سعر التوصيل لمحل بعينه من إدارة المحلات (طلب 2026-09-03) — الافتراضي مقفول والسعر سعر المنطقة',

        'GET /api/store/addresses' =>
            'قايمة عناوين المحل المحفوظة — «عناويني» زي تطبيق العملاء (طلب 2026-09-02)',
        'POST /api/store/addresses' =>
            'إضافة عنوان محفوظ للمحل — مع تصفير الافتراضي القديم في نفس المعاملة',
        'PUT /api/store/addresses/{}' =>
            'تعديل جزئي لعنوان المحل — نفس قاعدة array_key_exists بتاعة العميل',
        'DELETE /api/store/addresses/{}' =>
            'حذف عنوان المحل — الـWHERE على user_id هو حارس الملكية',

        /* 🔐 شاشة صلاحيات برنامج «تقفيل الطيارين» (طلب صاحب النظام 2026-09-01:
           «عايز أعمل تحكم من تطبيق تقفيل الطيارين بشكل كامل زي ما روح دمشق
           التطبيق بتاعها فيه تحكم»). نفس نموذج `rd_perms`، بس الفرض هنا في
           `month` و`deferredList` وكل نقط الكتابة — مش في القراءة لوحدها. */
        'GET /api/shifts/{id}/closeout-details' =>
            'تفاصيل تقفيلة الوردية للتقرير (طلب صاحب النظام 2026-09-01: تفاصيل العهدة — استلم كام وسلّم كام — والأذونات تظهر في التقفيلة) — قراءة خالصة من custody_transactions وpilot_leave_requests وعمولات التقفيلة',

        'GET /api/pilot-accounting/staff-month' =>
            'تقفيلة الموظفين (طلب صاحب النظام 2026-09-01: «زي ما الطيارين لهم تقفيلة ضيف كل الموظفين بشكل أوتوماتيك») — الحضور من جلسات المتصفح في attendance_sessions، والحسابات نفس معادلات دمشق المنقولة للطيارين',

        'POST /api/pilot-accounting/staff-entry' =>
            'تدخّل يدوي في خانة من شيت الموظف — نفس عقد pilot-accounting/entry',

        'POST /api/pilot-accounting/staff-perms' =>
            'فترات استئذان يدوية ليوم موظف — بتستبدل فجوات الجلسات التلقائية',

        'GET /api/pilot-accounting/acl' =>
            'تعريف مفاتيح الصلاحيات + المحفوظ لكل مستخدم + الفروع — الشاشة بتتبني من الرد ده عشان مايحصلش اختلاف بين اللي الأدمن بيعلّم عليه واللي السيرفر بيفهمه',

        'PUT /api/pilot-accounting/acl' =>
            'حفظ صلاحيات مستخدم — القيمة true بس هي اللي بتتخزّن، والمفتاح المش معروف بيتترمي',

        'DELETE /api/pilot-accounting/acl/{userId}' =>
            'رجوع للافتراضي — مسح الصف مش بيقفل عليه، بيرجّعه للي بيعمله دلوقتي (كل حاجة ماعدا السلف والقفل والإعدادات)',
        'POST /api/pilots/{id}/archive' =>
            'أرشفة الطيار — بديل الحذف (قرار صاحب النظام 2026-09-01: «بدل مسحه ينتفي لصفحة تانية عشان يبقى غير فعّال، وأي بيانات مرتبطة بيه ماتأثرش على البيانات السابقة»). الحذف مكانش شغّال أصلًا: pilots عليه ١٨ مفتاح أجنبي RESTRICT وكل طيار عنده حساب دخول مربوط. بيرفض لو عليه عهدة أو وردية مفتوحة أو أوردرات جارية، وبيقفل حساب دخوله.',

        'POST /api/pilots/{id}/unarchive' =>
            'رجوع الطيار المؤرشف للخدمة — الأدمن بس، وبيرجّع assigned_branch_id لفرعه الثابت ويفك قفل حسابه',

        'DELETE /api/customer/receivers/{id}' =>
            'حذف مستلم محفوظ من تطبيق العميل — الجدول والقايمة والحفظ كانوا موجودين من الترحيل، لكن مفيش حذف. اتضاف 2026-08-30 مع شاشة «عملائي» عشان التاجر يشيل مستلم مابيتعاملش معاه تاني. الحارس على customer_id: محدش يمسح مستلم حد تاني.',

        'POST /api/public/contact-message' =>
            'استقبال رسالة من نموذج «اتصل بنا» في الموقع العام — النموذج ده اتضاف بعد الترحيل ومكانش موجود في النظام القديم',

        'GET /api/contact-messages' =>
            'عرض رسايل التواصل الواردة في لوحة الإدارة — الجهة المقابلة للنموذج العام',

        'PUT /api/contact-messages/{id}/read' =>
            'تعليم رسالة تواصل كـ«مقروءة» بعد ما الإدارة تفتحها',

        'GET /api/order-notifications' =>
            'قايمة رسايل إشعار المستلمين بالأوردر (جدول order_notifications) — الإشعار نفسه اتضاف بعد الترحيل، النظام القديم مكانش بيسجّل الرسايل أصلًا',

        'POST /api/order-notifications/{id}/sent' =>
            'تعليم رسالة إشعار إنها اتبعتت بإيد الموظف — الوضع اليدوي المؤقت لحد ما حساب WhatsApp Cloud API يجهز',

        'GET /api/customer/push/key' =>
            'مفتاح VAPID العام لإشعارات ستارة الهاتف في تطبيق العملاء (Web Push) — الميزة اتضافت بعد الترحيل، النظام القديم كان بيستطلع بس',

        'POST /api/customer/push/subscribe' =>
            'تسجيل/تحديث اشتراك جهاز العميل في إشعارات الستارة (جدول customer_push_subscriptions) — upsert على الـendpoint',

        'POST /api/customer/push/unsubscribe' =>
            'حذف اشتراك جهاز العميل من إشعارات الستارة — الجهة المقابلة للاشتراك',

        'GET /api/pilot-commission-adjustments' =>
            'قايمة تعديلات عمولة الطيار — الكيان ده اتضاف 2026-08-26 بقرار صاحب النظام (أوردرات السفر بتتحسب نص الخدمة، وتعويض الشكوى بياخد عمولة بلا أوردر). النظام القديم كان بيحسب العمولة لحظيًا وبس',

        'POST /api/pilot-commission-adjustments' =>
            'تسجيل/تعديل عمولة مكتوبة بالإيد لأوردر، أو مبلغ عمولة مستقل بلا أوردر',

        'DELETE /api/pilot-commission-adjustments/{id}' =>
            'شيل التعديل ورجّع الأوردر للحساب التلقائي بنسبة الطيار',

        'GET /api/senders/{id}/custody' =>
            'بوابة العهدة لجهة الاستلام (كمّل 10 أوردرات متسلّمة؟ وسقف 3000 ج.م) — قيد اتفرض على الكول سنتر بعد الترحيل بقرار صاحب النظام، النظام القديم مكانش بيقيّد العهدة أصلًا',

        /* ── 💰 قسم حسابات الطيارين (2026-08-27) ──
           صاحب النظام طلب نموذج تقفيل «روح دمشق» بالظبط لطياري الشركة، بفرق
           إن الخانات بتتملى تلقائي من الورديات والأوردرات والعمولة. النظام
           القديم مكانش فيه القسم ده للشركة خالص — التقفيلة كانت شهرية بس
           (pilot_monthly_closeouts) ومفيهاش شيت يومي ولا سلف مؤجلة. */

        'POST /api/pilot-accounting/payout' =>
            'صرف راتب طيار/موظف من الخزنة على صافي اللقطة المعتمدة (الشهر لازم يكون مقفول) — صف في pilot_acct_payouts + حركة out في cash_transactions (طلب 2026-09-04)',
        'DELETE /api/pilot-accounting/payout/{}' =>
            'إلغاء صرفة راتب — الفلوس بترجع للخزنة بحركة in مرتبطة',

        'GET /api/pilot-accounting/reports' =>
            'الواقع قصاد المتوقع لكل فرع + الإجمالي: الفعلي من التقفيلات والمصروفات المصنّفة، المتوقع لحد النهارده، التوقّع لآخر الشهر، وسلسلة يومية للرسوم (المرحلة ٢ — 2026-09-05)',
        'GET /api/pilot-accounting/budget' =>
            'صفحة التقارير: الميزانية المتوقعة لكل فرع + الإجمالي ونقطة التعادل (طلب 2026-09-05)',
        'POST /api/pilot-accounting/budget' =>
            'بند ميزانية (إنشاء/تعديل) أو افتراض الفرع (أوردرات/يوم ومتوسط السعر) — act.budget إدارية',
        'DELETE /api/pilot-accounting/budget/{}' =>
            'حذف بند ميزانية',
        'POST /api/pilot-accounting/budget/prefill' =>
            'ملء ميزانية الفرع بالافتراضي من الحقيقي: كل موظف وطيار بسعره، العمولة ورسوم التطوير لكل أوردر، وصفوف الإيجار والمرافق',

        'GET /api/pilot-accounting/pilot-orders' =>
            'أوردرات الطيار في اليوم التجاري بعمولة كل أوردر (التلقائية والمكتوبة) — شاشة «اضغط على الطيار تشوف أوردراته وتحط العمولة» (طلب 2026-09-04). الكتابة على pilot-commission-adjustments الموجود',

        'POST /api/pilot-accounting/day-summary' =>
            'بلوك تقفيلة الفرع اليومي زي «تقفيل روح دمشق» من غير النسبة (طلب صاحب النظام 2026-09-04): الخارجي والمصاريف والمستلم فعلًا من المشرف لكل فرع ويوم — المطابقة بتتحسب على السيرفر في رد month',

        'GET /api/pilot-accounting/month' =>
            'الشهر كامل في نداء واحد: مصفوفة الأيام لكل طيار + إجماليات الشهر + السلف المؤجلة + حالة القفل. نداء واحد عشان الشيت اليومي وكشف الطيار والتقفيلة يبقوا نفس الحسبة مش تلات حسبات بتختلف',

        'GET /api/pilot-accounting/deferred' =>
            'قايمة السلف المؤجلة بأقساط الشهر المطلوب والمتبقي على كل سلفة',

        'GET /api/pilot-accounting/settings' =>
            'إعدادات القسم: بداية اليوم التجاري (9 ص بقرار صاحب النظام) وساعات الوردية المعيارية',

        'POST /api/pilot-accounting/entry' =>
            'تدخّل يدوي على خانة في الشيت اليومي — القيمة الفاضية بتشيل الـoverride والرقم بيرجع محسوب من النظام',

        'POST /api/pilot-accounting/perms' =>
            'استبدال فترات الاستئذان المكتوبة بالإيد ليوم واحد',

        'POST /api/pilot-accounting/deferred' =>
            'تسجيل أو تعديل سلفة مؤجلة بقسط شهري — قرار مالي فللإدارة بس',

        'DELETE /api/pilot-accounting/deferred/{id}' =>
            'إلغاء سلفة مؤجلة بالكامل مع أقساطها',

        'POST /api/pilot-accounting/deferred/{id}/payment' =>
            'قسط شهر بعينه بيغلب القسط الافتراضي — والمبلغ الفاضي بيرجّع الافتراضي',

        'POST /api/pilot-accounting/lock' =>
            'قفل الشهر المالي — بعده الشيت والتقفيلة قراءة فقط. للفرع أو للشركة كلها',

        'DELETE /api/pilot-accounting/lock' =>
            'فتح شهر مقفول تاني',

        'PUT /api/pilot-accounting/settings' =>
            'تعديل إعدادات القسم',

        /* ── 🚨 لوحة أعطال الإنتاج (2026-08-27) ──
           اتضافت بعد ما باج في تطبيق العميل قعد 3 أيام مكتوب في اللوج
           ومحدش شافه. النظام القديم مكانش فيه أي طبقة إنذار. */

        'GET /api/alerts' =>
            'أعطال الإنتاج مجمّعة بالبصمة مع عدّاد التكرار وحالة إرسال التنبيه',

        'POST /api/alerts/{id}/resolve' =>
            'تعليم العطل إنه اتصلّح — بيختفي من القايمة النشطة',

        'POST /api/alerts/{id}/reopen' =>
            'رجّع عطل متعلّم متصلّح للقايمة النشطة — لازم يكون موجود عشان العطل اللي بيرجع بعد «إصلاح» يبان',

        /* ── ⏰ استعجال الأوردر من بوابة المحل (2026-08-29) ──
           صاحب النظام طلب زرار «الأوردر جاهز — برجاء الاستعجال». النظام
           القديم مكانش فيه أي قناة للمحل يستعجل بيها. */

        'POST /api/orders/{id}/urge' =>
            'المحل بيستعجل أوردره — التنبيه بيروح للفرع لو الأوردر لسه في المكتب وللطيار لو متحمّل عليه، بحد أقصى استعجال كل 4 دقايق',

        'GET /api/orders/urges' =>
            'الاستعجالات اللي تخص المستخدم — الفرع بياخد بتاعة فرعه والطيار بتاعته هو (آخر 12 ساعة)',

        'POST /api/orders/urges/seen' =>
            'تعليم تنبيهات الاستعجال مقروءة',

        /* ✅ تأكيد المحل إنه سلّم الأوردر للطيار — 2026-08-29.
           النظام القديم كان بيعتبر «جاري التوصيل» بتبدأ من إسناد الطيار،
           فالمحل مكانش عنده أي طريقة يقول «الطيار شالها من عندي». */
        'POST /api/orders/{id}/handover' =>
            'المحل بيأكّد إنه سلّم الأوردر للطيار — بيفصل «الطيار جاي يستلم» عن «الأوردر مع الطيار» في شاشة المحل',

        /* 🔄 تحويل بين الخزن — 2026-08-29. النظام القديم مكانش فيه غير
           وارد/منصرف على خزنة واحدة، فنقل فلوس بين خزنتين كان بيتعمل
           بحركتين منفصلتين وأي فشل بينهم بيسيب فرق في الدفاتر. */
        'POST /api/cash-stores/transfer' =>
            'تحويل مبلغ من خزنة لخزنة في معاملة واحدة — بيسجّل صادر ووارد مربوطين باسم الخزنة التانية',
    ];

    protected $signature = 'route:coverage {--missing : اعرض الناقص بس} {--extra : اعرض الزيادة بس}';

    protected $description = 'بيقيس تغطية ترحيل المسارات مقابل النظام القديم';

    public function handle(): int
    {
        $legacyFile = dirname(base_path()) . '/aldahshan/public/index.php';
        if (! is_file($legacyFile)) {
            $this->error("مالقيتش جدول المسارات الأصلي: {$legacyFile}");
            return self::FAILURE;
        }

        // استخراج مفاتيح مصفوفة $routes: 'GET /api/xxx' => 'handler'
        preg_match_all(
            "/^\s*'(GET|POST|PUT|DELETE|PATCH)\s+([^']+)'\s*=>\s*/m",
            (string) file_get_contents($legacyFile),
            $m,
            PREG_SET_ORDER
        );

        $legacy = [];
        foreach ($m as $r) {
            $legacy[strtoupper($r[1]) . ' ' . $this->norm($r[2])] = true;
        }

        $mine = [];
        foreach (Route::getRoutes() as $route) {
            $uri = $route->uri();
            if (! str_starts_with($uri, 'api/')) {
                continue;
            }
            if (str_contains($uri, 'fallbackPlaceholder')) {
                continue;
            }
            foreach ($route->methods() as $method) {
                if (in_array($method, ['HEAD', 'OPTIONS'], true)) {
                    continue;
                }
                $mine[$method . ' ' . $this->norm('/' . $uri)] = true;
            }
        }

        // القايمة البيضا بتتوحّد بنفس قواعد أي مسار تاني عشان المقارنة تمشي على الشكل مش التسمية
        $allowed = [];  // مفتاح موحّد => المفتاح زي ما هو مكتوب في الثابت
        foreach (array_keys(self::INTENTIONAL_ADDITIONS) as $key) {
            [$method, $path] = explode(' ', $key, 2);
            $allowed[strtoupper($method) . ' ' . $this->norm($path)] = $key;
        }

        $missing = array_diff_key($legacy, $mine);

        // كل اللي في لارافل ومش في الأصل، بعدين بنقسمه: زيادة محتاجة مراجعة / إضافة مقصودة
        $unmatched  = array_diff_key($mine, $legacy);
        $extra      = array_diff_key($unmatched, $allowed);
        $intentional = array_intersect_key($allowed, $unmatched);

        // مسار في القايمة البيضا ومش متسجّل في لارافل خالص — القايمة بتتحوّل لمقبرة
        $ghosts = array_diff_key($allowed, $mine);

        // التغطية والناقص زي ما هما — بيقيسوا الترحيل بس، والإضافات المقصودة مالهاش دخل بيهم
        $done    = count($legacy) - count($missing);
        $pct     = count($legacy) > 0 ? round($done / count($legacy) * 100) : 0;

        $showMissing = $this->option('missing') || ! $this->option('extra');
        $showExtra   = $this->option('extra') || ! $this->option('missing');

        if ($showMissing && $missing) {
            $this->newLine();
            $this->line('── ناقص (' . count($missing) . ') ──');
            $byPrefix = [];
            foreach (array_keys($missing) as $k) {
                [$mth, $path] = explode(' ', $k, 2);
                $seg = explode('/', trim($path, '/'))[1] ?? '?';
                $byPrefix[$seg][] = $k;
            }
            ksort($byPrefix);
            foreach ($byPrefix as $seg => $items) {
                $this->line(sprintf('  %-18s %d', $seg, count($items)));
                foreach ($items as $k) {
                    $this->line('       ' . $k);
                }
            }
        }

        if ($showExtra && $extra) {
            $this->newLine();
            $this->line('── زيادة عن الأصل (' . count($extra) . ') — راجعها، غالبًا غلطة اسم ──');
            foreach (array_keys($extra) as $k) {
                $this->line('  ' . $k);
            }
        }

        if ($showExtra && $intentional) {
            $this->newLine();
            $this->line('── إضافات مقصودة (' . count($intentional) . ') — مش محسوبة زيادة ──');
            foreach ($intentional as $key) {
                $this->line('  ' . $key);
                $this->line('       ' . self::INTENTIONAL_ADDITIONS[$key]);
            }
        }

        // تحذير بيتعرض دايمًا مهما كانت الفلاتر: القايمة البيضا نفسها بقت فيها مسارات ميتة
        if ($ghosts) {
            $this->newLine();
            $this->warn('── مسارات في القايمة البيضا ومش متسجّلة في لارافل (' . count($ghosts) . ') ──');
            $this->warn('   يا تترجّع يا تتشال من INTENTIONAL_ADDITIONS — القايمة مش مقبرة:');
            foreach ($ghosts as $key) {
                $this->warn('  ' . $key);
                $this->warn('       ' . self::INTENTIONAL_ADDITIONS[$key]);
            }
        }

        $this->newLine();
        $this->line('════════════════════════════════════════════');
        $line = sprintf('التغطية: %d / %d مسار  (%d%%)  ·  ناقص %d  ·  زيادة %d  ·  إضافات مقصودة %d',
            $done, count($legacy), $pct, count($missing), count($extra), count($intentional));
        $missing === [] && $extra === [] && $ghosts === [] ? $this->info($line) : $this->line($line);
        $this->line('════════════════════════════════════════════');

        return self::SUCCESS;
    }

    /** توحيد شكل المسار: {أي اسم} → {} عشان المقارنة تبقى على الشكل مش التسمية */
    private function norm(string $path): string
    {
        return preg_replace('/\{[^}]+\}/', '{}', rtrim($path, '/')) ?? $path;
    }
}
